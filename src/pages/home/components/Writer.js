import React, { Component } from 'react'
import {
    WriterWrapper
} from '../style'

class Writer extends Component {
    render() {
        return (
            <WriterWrapper>Writer_homework</WriterWrapper>
        )
    }
}

export default Writer
